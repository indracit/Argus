const insight_queries = {
    test : 'SELECT JSON_OBJECT(*) FROM txn_txnsummary'

}


const report_queries = {
    test : 'SELECT JSON_OBJECT(*) FROM txn_txnsummary'
}


module.exports = {insight_queries,report_queries}